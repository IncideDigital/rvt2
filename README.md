# Revealer Toolkit 2

A forensic tool to rule them all.

# Install

Use the project directory as the virtualenv (highly recommended)

``` 
> sudo apt-get install python3-pipenv
> pip3 install --user pipenv
> env PIPENV_VENV_IN_PROJECT=1 pipenv --python /usr/bin/python3
> pipenv install
```

